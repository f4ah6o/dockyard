# Copy to config/agents.env and adjust to your local CLIs.
# For Claude Code via z.ai, export ZAI_API_KEY in your shell before running dock commands.

ORCH_DECIDE_CMD="claude -p"
STRATEGIST_CMD="gemini -p"
REVIEWER_CMD="gemini -p"
RUNNER_DEFAULT_CMD="claude -p"

# Optional resident attach commands for tmux panes.
ATTACH_ORCH_CMD="claude"
ATTACH_STRATEGIST_CMD="gemini"
ATTACH_REVIEWER_CMD="gemini"
ATTACH_RUNNER_CMD="claude"
